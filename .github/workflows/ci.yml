name: Golang CI

on:
    push:
        branches:
            - master
    workflow_dispatch:
jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Set up Go
              uses: actions/setup-go@v4
              with:
                  go-version: 1.26

            - name: Run tests
              run: go test ./...
            
            - name: Build app
              run: go build -o myapp .
